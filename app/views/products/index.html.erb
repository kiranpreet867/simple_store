<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
  <%= link_to product.title, "/products/#{product.id}" %> -
  $<%= product.price %> -
  Stock: <%= product.stock_quantity %>
</li>
  <% end %>
</ul>
